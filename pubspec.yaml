name: quiz_mater_apllication
description: "Quiz Master - Ứng dụng luyện thi trắc nghiệm THPT"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase
  # firebase_core: ^3.12.1
  # firebase_auth: ^5.5.4
  # cloud_firestore: ^5.6.8
  # firebase_storage: ^12.4.4
  # cloud_functions: ^5.3.4

  # Kiến trúc
  flutter_bloc: 
  go_router: ^17.2.3
  get_it: ^8.0.3
  dartz: ^0.10.1
  equatable: ^2.0.7

  # Giao diện
  google_fonts: ^8.1.0
  flutter_math_fork: ^0.7.2
  fl_chart: ^0.70.2
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  flutter_native_splash: 

  # Tiện ích
  flutter_dotenv: ^5.2.1
  connectivity_plus: ^6.1.4
  shared_preferences: ^2.5.3
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: 
  mocktail: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    # - .env
